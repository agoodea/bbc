export default {
    state: {
        actionState: ""
    },
    mutations: {
        setActionState(state, actionState_) {
            state.actionState = actionState_;
        }
    }
}
// main - main tab / menu (add slide) camera || galery, 
// Albums tab -
// Gallery tab - 
// Settings/user tab - 
// Create slide tab -  add record -> save